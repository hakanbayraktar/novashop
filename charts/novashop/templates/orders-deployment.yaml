{{- if .Values.orders.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "novashop.fullname" . }}-orders
  labels:
    {{- include "novashop.labels" . | nindent 4 }}
    app.kubernetes.io/component: orders
spec:
  replicas: {{ .Values.orders.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "novashop.name" . }}-orders
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "novashop.name" . }}-orders
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: orders
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        runAsNonRoot: {{ .Values.orders.securityContext.runAsNonRoot }}
        runAsUser: {{ .Values.orders.securityContext.runAsUser }}
      containers:
        - name: orders
          image: "{{ .Values.orders.image.repository }}:{{ .Values.orders.image.tag }}"
          imagePullPolicy: {{ .Values.orders.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: RETAIL_ORDERS_PERSISTENCE_PROVIDER
              value: "in-memory"
            - name: RETAIL_ORDERS_MESSAGING_PROVIDER
              value: "in-memory"
            - name: SERVER_TOMCAT_ACCESSLOG_ENABLED
              value: "true"
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=75.0"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          resources:
            {{- toYaml .Values.orders.resources | nindent 12 }}
{{- end }}
